const categoryData = require("./categories");
const userData = require("./users");
const reviewData = require(`./reviews`);
const commentData = require(`./comments`);

module.exports = { categoryData, userData, reviewData, commentData };
